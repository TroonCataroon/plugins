name: Validate plugins

on:
  pull_request:
    paths:
      - ".cursor-plugin/marketplace.json"
      - ".agents/plugins/marketplace.json"
      - "**/plugin.json"
      - "schemas/**"
      - "scripts/validate-plugins.mjs"
      - "scripts/validate-openai-plugins.mjs"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install --no-save ajv ajv-formats

      - name: Validate Cursor plugin definitions
        run: node scripts/validate-plugins.mjs

      - name: Validate OpenAI plugin definitions
        run: node scripts/validate-openai-plugins.mjs
